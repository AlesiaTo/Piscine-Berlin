#ifconfig -a | grep ether | cut -d, -f 3
#ifconfig -a | grep 'ether' | tr -s | cut -f 3
#ifconfig -a | grep HWaddr
#ifconfig eth0
#ifconfig -a | grep ether 
#--extended-regexp  
#ip -brief link maddress
#ifconfig eth9 | awk '/ether/ {print $2}' 
#ifconfig -a | greb ether 
# "Ethernet"
~                  
